---
deployment:
  tasks:
    - export DEPLOYPATH=/home7/geraldna/public_html/
    - /usr/bin/rsync -av --delete --exclude='.git/' --exclude='.cpanel.yml' --exclude='.well-known/' ./ "$DEPLOYPATH"
